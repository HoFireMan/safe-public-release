[project]
name = "safe-public-release"
version = "2.0.0"
description = "Prepare a clean public candidate from a private Git repository"
requires-python = ">=3.10"
dependencies = []

[tool.safe-public-release]
version = "2"
workflow = "private-to-public"
